[package]
name = "nexus_security"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
zeroize = { version = "1.7", features = ["derive"] }
tracing = "0.1"
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }

[lib]
path = "src/lib.rs"
